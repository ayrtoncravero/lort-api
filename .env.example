PORT=3000
NODE_ENV=development
# Comma-free single origin or "*". Set to the real frontend origin in production
# (e.g. https://lort-app.example.com); "*" is fine for local development.
CORS_ORIGIN=*
